
package tn.farah.NetflixJava.utils;

public enum Screen {
    // Authentification & Profils
    login, logup, oublie, ResetPassword, pofiles, addProfile,
    signup1, signup2, signup3, ManageProfiles,

    // Navigation Client
    mainView, home, films, series, search, myList, notification,

    // Détails & Lecture
    detail, detailFilm, episodeView, episodeComments, video, filmPlayer, 
    HomePage2, DetailMedia, detailMovie2,

    // Administration
    AdminDashboard, admin_main, admin_movie_item, admin_series_item,
    manageUsers, ManageSeries, CommentaireAdmin, notificationAdmin
}